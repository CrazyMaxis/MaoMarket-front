export interface INews {
  id: string;
  title: string;
  body: string;
  image: string;
  createdAt: string;
}

export interface INewsList {
  id: string;
  title: string;
  body: string;
  image: string;
}
