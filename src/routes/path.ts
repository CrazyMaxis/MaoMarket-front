export enum PATH {
  HOME = '/',
  AUTHARIZATION = '/auth',
  PROFILE = '/profile',
  CAT = '/cat',
  NEWS = '/news',
}

export enum PATH_AUTHORIZATION {
  LOGIN = 'login',
  REGISTER = 'register',
  VERIFY = 'verify',
}
