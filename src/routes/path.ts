export enum PATH {
  HOME = '/',
  AUTHARIZATION = '/auth',
  PROFILE = '/profile',
  CAT = '/cat',
}

export enum PATH_AUTHORIZATION {
  LOGIN = 'login',
  REGISTER = 'register',
  VERIFY = 'verify',
}
